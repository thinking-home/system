# Результат — planner / propose (r3)

## Сводка

- Отработан возврат с гейта: в `openspec/changes/chat-list-ui/proposal.md` добавлен результат «список выводится по убыванию даты — сверху самые новые чаты», порядок фиксирован и обеспечивается самим разделом; из раздела «Не входит» убрана сортировка по дате. За границами остались управление порядком (сортировка по другим полям, переключение направления, запоминание выбора пользователя), пагинация, фильтрация и поиск.
- Контракт HTTP API списка чатов не меняется: параметры сортировки в него не добавляются, раздел не полагается на порядок записей в ответе. Состав функциональностей прежний — одна новая capability `plugins/telegram-chat-list/web-ui`, теперь с требованием о порядке вывода по дате.
- `cow validate --change chat-list-ui --json`: `valid: true`; остались только info о заблокированных артефактах следующих фаз и warning `DELTA_MISSING` (дельты specs пишутся на фазе plan).
- Размер `normal`, сложность реализации и ревью обычная: прецедент добавления UI-плагина в репозитории есть (коммит `69745f3`, cron web-ui), сортировка по дате не добавляет контрактов и миграций.

```yaml
# cow-result
status: утверждение
blocker: { category: нет, message: "" }
size: normal
complexity: { implementation: обычная, review: обычная }
skip_specs: false
```
