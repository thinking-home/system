## 1. plugins/timer/callback-scheduling

- [x] 1.1 Сверить требования спецификации `specs/plugins/timer/callback-scheduling/spec.md` с `ThinkingHome.Plugins.Timer/TimerPlugin.cs`, `InternalTimer.cs`, `TimerCallbackAttribute.cs`, `TimerCallbackDelegate.cs`; убедиться, что нет расхождений с текущим поведением

## 2. Завершение

- [x] 2.1 Прогнать `npx openspec validate describe-timer-plugin-spec --strict` и убедиться, что все спецификации проходят валидацию без ошибок
