# Тестировщик: покрытие изменения `chat-list-ui-c`

## Ход работы

1. Прочитал дельты спецификаций: `specs/plugins/telegram-chat-list/http-api/spec.md` (MODIFIED),
   `specs/plugins/telegram-chat-list/web-ui/spec.md` (ADDED), `specs/plugins/web-ui/application-shell/spec.md` (ADDED),
   а также `design.md` и `.cow/project/testing.md`.
2. Собрал полный список утверждений (10 сценариев в 3 капабилити, все — added/modified):
   - `plugins/telegram-chat-list/http-api` › «Список сохранённых чатов» — 3 сценария (запрос списка, отсутствующие значения, порядок по `date`);
   - `plugins/telegram-chat-list/web-ui` — 4 требования, 7 сценариев (зависимость от HTTP API, раздел списка, отображение null/пустого списка, ошибка загрузки);
   - `plugins/web-ui/application-shell` › «Ссылка на раздел списка чатов на корневой странице» — 1 сценарий.
3. Для каждого утверждения проверил возможность автоматизации по `testing.md` и разделу «Стратегия проверки» `design.md`:
   - серверная часть (`http-api`) требует реальной БД (`DatabasePlugin.OpenSession()`, сортировка выполняется в LINQ-запросе, а не в выделяемой чистой функции) — в проекте нет инфраструктуры component-тестов с PostgreSQL (`testing.md`, строка «component (C#, плагин с зависимостями)»);
   - клиентская часть (`web-ui`, ссылка в `application-shell`) требует браузера — тест-раннера для `ThinkingHome.Plugins.*/frontend/**` и e2e-тестов в проекте нет (`testing.md`, строки «unit и component (TypeScript)» и «e2e», а также таблица «Не автоматизируется» → «Веб-интерфейс: разделы, тема, уведомления, SignalR»).
   - Это совпадает с собственным разделом «Стратегия проверки» `design.md`, где автор дизайна уже зафиксировал: автотестов не планируется, только `npx tsc` и `dotnet build`/`dotnet test` регрессии. Расхождений между дизайном и тестовой стратегией нет, блокер не требуется.
   - Добавление нового тест-раннера (например, vitest) для этой единственной задачи посчитал расширением объёма задачи за рамки утверждённого дизайна и правил проекта — не стал этого делать.
4. Все 10 утверждений отмечены `manual` с причиной в `coverage.yaml`.
5. Поскольку все утверждения — manual, заполнил `test-plan.md`: по одному пункту ручной проверки на каждое утверждение (шаги, ожидаемый результат, окружение — локальный PostgreSQL + `dotnet run`) и чеклист перед мержем (ручные проверки, `dotnet test`, `dotnet build`, `npx tsc`, проверка схемы URL).
6. Новых тестовых файлов не создавал (автоматизируемых утверждений нет), продуктовый код не менял.
7. Запустил регрессию: `dotnet test ThinkingHome.Tests/ThinkingHome.Tests.csproj` — 39/39 зелёных, без изменений (ожидаемо, т.к. новых тестов нет).

## Блокер

Не требуется: тестовая стратегия `design.md` уже сама объясняет отсутствие автотестов и совпадает с ограничениями `testing.md`; противоречий со спецификациями нет.

```yaml
# cow-result
status: готово
blocker: { category: нет, artifact: "", message: "" }
protected: []
verified:
  - "dotnet test ThinkingHome.Tests/ThinkingHome.Tests.csproj — 39 регрессионных тестов проходят, новых автотестов не добавлено (все 10 утверждений дельт помечены manual в coverage.yaml с причиной — нет component-тестов с БД и нет тест-раннера/e2e для веб-интерфейса, testing.md)"
```
