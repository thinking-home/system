# Tester report: chat-list-ui-c (r4, phase cover)

## Разбор утверждений

Дельта `openspec/changes/chat-list-ui-c/specs/plugins/telegram-chat-list/web-ui/spec.md` вводит 5 требований и 7 сценариев — все про поведение веб-раздела в браузере (запрос к HTTP API, отображение таблицы, пустые значения, пустой список, ошибка загрузки, локализация, ссылка с корневой страницы).

По `testing.md`:
- нет тест-раннера для `ThinkingHome.Plugins.*/frontend/**` (unit/component TypeScript — прочерк);
- нет component-тестов C# с реальными плагинами/Kestrel/PostgreSQL;
- нет e2e-тестов и среды для них;
- таблица «Не автоматизируется» прямо перечисляет «Веб-интерфейс: разделы, тема, уведомления, SignalR» с причиной «браузерных тестов нет; проверять в браузере».

`design.md` в разделе «Стратегия проверки» подтверждает то же: unit (C#) — тестируемой логики в разделе нет (регистрация страницы — атрибут и константы); TypeScript — только `npx tsc` (проверка типов, не тест утверждений); сборка и регрессия — `dotnet build`/`dotnet test`; ручная проверка — вне задач реализации.

Соответственно ни для одного из 7 утверждений нет применимого автоматизируемого уровня — все помечены `manual` с причиной со ссылкой на `testing.md`. Новых тестовых файлов не создавалось (нет продуктовой логики, поддающейся unit/component/e2e-тестам, и нет инфраструктуры для этого в проекте).

## Заполненные артефакты

- `openspec/changes/chat-list-ui-c/coverage.yaml` — capability `plugins/telegram-chat-list/web-ui`, все 5 групп / 7 утверждений размечены `manual` с причиной.
- `openspec/changes/chat-list-ui-c/test-plan.md` — по одному пункту ручной проверки на каждое manual-утверждение (шаги, ожидаемый результат, окружение из `testing.md` «Среда e2e») плюс чеклист перед мержем, включающий `npx tsc`, `dotnet build`, `dotnet test`.

## Проверено

- `dotnet test ThinkingHome.Tests/ThinkingHome.Tests.csproj` — 39 регрессионных тестов проходят (без изменений, изменение не затрагивает C#-логику вне UI-плагина).
- Новых автотестов не писалось (нет утверждений, для которых в проекте есть тестируемый уровень); TS-проекта раздела ещё не существует (создаётся реализатором), поэтому `npx tsc` для него не запускался — это задача 5.1 в `tasks.md`, выполняемая реализатором/верификатором после появления кода.

```yaml
# cow-result
status: готово
blocker: { category: нет, artifact: "", message: "" }
protected: []
verified:
  - "dotnet test ThinkingHome.Tests/ThinkingHome.Tests.csproj — 39 регрессионных тестов проходят, автотесты по дельте не создавались (все 7 утверждений — manual по testing.md, «Не автоматизируется»: «Веб-интерфейс: разделы, тема, уведомления, SignalR»)"
```
