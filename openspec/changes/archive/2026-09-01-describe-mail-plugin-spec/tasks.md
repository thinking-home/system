## 1. plugins/mail/mail-sending

- [x] 1.1 Сверить требования спецификации `specs/plugins/mail/mail-sending/spec.md` с `ThinkingHome.Plugins.Mail/MailPlugin.cs`; убедиться, что нет расхождений с текущим поведением

## 2. Завершение

- [x] 2.1 Прогнать `npx openspec validate describe-mail-plugin-spec --strict` и убедиться, что все спецификации проходят валидацию без ошибок
